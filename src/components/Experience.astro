---

const exper = [
    {
        fecha: 'Actualmente',
        puesto: 'Freelance programador full stack',
        parrafo: ['Desarrollador de interfaces frontend con html, css, tailwindcss. Con javascript , typescript como lenguaje.', 'Bibliotecas como React js, y frameworks  como Astro js, Next js.', 'Del lado del servidor con Node js, express y como base de datos relacional tursoSql y no relacional strapi']
    },
    {
        fecha: 'Marzo 2023 - Julio 2023',
        puesto: 'programador web',
        parrafo: ['Fui integrante como desarrollador frontend en la startup identikit app.', 'Estuve con un contrato de prueba, desarrollamos una landing page para una aseguradora', 'Usamos herramientas como react js, react router dom, tailwind']
    }
];

---

<section class="w-full max-h-full flex flex-col justify-start items-center" id="experience">
    <div class="w-10/12 h-full flex flex-1 flex-col justify-center items-center mt-8">
        <div class="flex flex-row gap-5">
            <img class="w-8 h-8" src="./office-job.svg" alt="job">
            <h2 class="font-semibold text-3xl text-center">Experiencia laboral</h2>
        </div>
        <div class="h-fit flex flex-col items-center mt-10">
            <div class="w-full h-fit flex flex-col gap-10 border-l border-black">
                {
                
                    exper.map(item => (
                        <article class="w-full h-fit relative -left-1.5 mt-2">
                            <div class="flex flex-row items-center gap-3">
                                <div class="w-3 h-3 rounded-full bg-blue-600 relative"></div>
                                <h4 class="text-blue-600 font-semibold text-xs">{item.fecha}</h4>                            
                            </div>
                            <div class="max-w-fit p-5">
                                <h4 class="text-orange-700 font-semibold text-xl">{item.puesto}</h4>
                                {
                                    item.parrafo.map((linea) => (
                                        <p class="max-w-96 text-xl text-wrap">{linea}</p>
                                    ))
                                }
                            </div>
                        </article>
                    ))
                }
            </div>
        </div>
    </div>
</section>